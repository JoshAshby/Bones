bones:
  repository_root: <%= ENV["REPO_ROOT"] %>
  fossil_binary: <%= ENV["FOSSIL_BINARY"] %>

database:
  url: <%= ENV.delete("DATABASE_URL") %>

domain: https://bones.isin.space
email_from: no-reply@bones.isin.space

mail:
  delivery_method: logger
  #delivery_method: smtp
  #delivery_config:
     #address: "localhost"
     #port: 1025

feature_flags:
  sign_up: false
